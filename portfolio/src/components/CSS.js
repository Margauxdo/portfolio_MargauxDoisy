import React from 'react';
import '../styles/components/CSS.css';

const CSS = () => {
    return (
        <div>
            <img src='./CCS.png' alt='CSS'/>
        </div>
    );
};

export default CSS;